artifacts = ["/app/report.json"]

[task]
name = "dynamo/log-report"
description = "Parse an Apache-style access log into a small JSON summary report."

[metadata]
category = "data_processing_and_etl"
subcategory = "text_processing"
task_objective = ["transform", "generate"]
artifact_type = ["text_or_log_file", "generated_output_artifact"]
expert_time_estimate_hours = 0.3
model_tested = "GPT-5.4"
agent_tested = "Terminus-2"
difficulty_explanation = "Parse an Apache-style access log and emit summary statistics as JSON. Straightforward everyday scripting."
solution_explanation = "Read /app/access.log line by line: count non-empty lines as requests, collect distinct first-field IPs, and tally request paths from each request line; write total_requests, unique_ips, and top_path to /app/report.json."
verification_explanation = "Parse /app/report.json and assert it is a JSON object with exactly total_requests==6, unique_ips==3, and top_path=='/index.html' — the values computed directly from the fixture. Existence-only checks are not sufficient; wrong values fail."

[verifier]
timeout_sec = 120.0

[agent]
timeout_sec = 120.0

[environment]
build_timeout_sec = 600.0
cpus = 1
memory_mb = 2048
storage_mb = 10240
gpus = 0
allow_internet = true
mcp_servers = []
